describe("Paginator", function() {
  var paginator;

  beforeEach(function() {
    paginator = {};
  });

  describe("when time in available", function() {
    it("should write tests here", function() {
      expect(false).not.toBe(false);
    });
  });
});